# Image-captioning-
this repo explores image captioning. Image Captioning enables transforming visuals into text.

# --------------------------------------------------------------------------------------

# 📘 Image Captioning System (BLIP + Gradio)

This project is a simple, clean, and efficient image captioning system built with:

BLIP (Bootstrapping Language-Image Pre-training) from Hugging Face

Gradio UI for easy interaction

Python + Transformers + PIL

It takes an input image (uploaded or from a URL) and generates a natural-language caption that describes the content of the image.
# --------------------------------------------------------------------------------------

# 🚀 Benefits

Easy to use — Upload an image and get a caption instantly

Fast — Uses BLIP-base model optimized for quick inference

Reusable — Captioning logic is separated into its own module (image_cap.py)

Extendable — You can later add:

batch captioning

improved UI

local GPU acceleration

deployment (HuggingFace Spaces, Docker, etc.)

# --------------------------------------------------------------------------------------
# add on 
A web sraper service enter the url of the webpage you want the scraper will go through all the pages look for <img>
and return back image captioning 